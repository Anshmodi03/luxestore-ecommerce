// Removed — AuthSync functionality is now built into FirebaseAuthProvider (context/FirebaseAuthContext.tsx)
// Firebase's onAuthStateChanged handles token wiring and auth state in one place.
export {}
